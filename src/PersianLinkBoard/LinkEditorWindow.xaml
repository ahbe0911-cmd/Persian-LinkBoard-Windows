<Window x:Class="PersianLinkBoard.LinkEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ویرایش لینک" Width="500" Height="430" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner" Background="{StaticResource PanelBrush}"
        FlowDirection="RightToLeft" ShowInTaskbar="False">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="22"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="14"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="14"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="14"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock x:Name="HeadingText" Text="افزودن لینک جدید" FontSize="24" FontWeight="Bold"/>
            <TextBlock Text="اطلاعات لینک را وارد کنید؛ همه موارد بعداً قابل ویرایش هستند."
                       Foreground="{StaticResource MutedBrush}" Margin="0,5,0,0" FontSize="12"/>
        </StackPanel>

        <StackPanel Grid.Row="2">
            <TextBlock Text="عنوان لینک" FontWeight="SemiBold" Margin="0,0,0,6"/>
            <TextBox x:Name="TitleBox" Height="44" VerticalContentAlignment="Center" MaxLength="80"/>
        </StackPanel>

        <StackPanel Grid.Row="4">
            <TextBlock Text="آدرس سایت" FontWeight="SemiBold" Margin="0,0,0,6"/>
            <TextBox x:Name="UrlBox" Height="44" VerticalContentAlignment="Center" FlowDirection="LeftToRight" TextAlignment="Left"/>
        </StackPanel>

        <StackPanel Grid.Row="6">
            <TextBlock Text="دسته‌بندی" FontWeight="SemiBold" Margin="0,0,0,6"/>
            <TextBox x:Name="CategoryBox" Height="44" VerticalContentAlignment="Center" MaxLength="40"/>
        </StackPanel>

        <Border Grid.Row="8" x:Name="ErrorBorder" Background="#3A1D2B" BorderBrush="#7C334D" BorderThickness="1"
                CornerRadius="10" Padding="10" Visibility="Collapsed">
            <TextBlock x:Name="ErrorText" Foreground="#FFB7C8" TextWrapping="Wrap"/>
        </Border>

        <Grid Grid.Row="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Content="انصراف" Margin="0,0,6,0" Click="Cancel_Click"/>
            <Button Grid.Column="1" Content="ذخیره لینک" Margin="6,0,0,0" Background="{StaticResource AccentBrush}" Click="Save_Click"/>
        </Grid>
    </Grid>
</Window>
